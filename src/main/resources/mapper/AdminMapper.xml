<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiyuan.mapper.AdminMapper">
    <select id="getAdminById" resultType="com.qiyuan.pojo.Admin">
        select *
        from admin
        where id = #{id}
    </select>

    <insert id="insertAdmin">
        insert into admin(account, password, nickname, status, avatar, email, last_login_time, update_time, create_time)
        values (#{account}, #{password}, #{nickname}, #{status}, #{avatar}, #{email}, #{lastLoginTime}, #{updateTime},
                #{createTime})
    </insert>

    <delete id="deleteAdminById">
        delete
        from admin
        where id = #{id}
    </delete>

    <select id="getAdminByCreateTime" resultType="com.qiyuan.pojo.Admin">
        select *
        from admin
        where create_time = #{createTime}
    </select>
    <update id="updateAdmin">
        update admin
        set
        <if test="account != null">
            account = #{account},
        </if>
        <if test="password != null">
            password = #{password},
        </if>
        <if test="nickname != null">
            nickname = #{nickname},
        </if>
        <if test="status != null">
            status = #{status},
        </if>
        <if test="avatar != null">
            avatar = #{avatar},
        </if>
        <if test="email != null">
            email = #{email},
        </if>
        <if test="superAdmin != null">
            super_admin = #{superAdmin},
        </if>
        update_time = #{updateTime}
        where id = #{id}
    </update>
    <select id="getAdminByAccount" resultType="com.qiyuan.pojo.Admin">
        select *
        from admin
        where account = #{account}
    </select>
    <select id="getAdminByNickname" resultType="com.qiyuan.pojo.Admin">
        select *
        from admin
        where nickname = #{nickname}
    </select>
</mapper>
