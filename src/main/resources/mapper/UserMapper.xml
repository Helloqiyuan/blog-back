<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiyuan.mapper.UserMapper">


    <insert id="insert">
        insert into user(username, password, email, last_login_time, update_time, create_time)
        values (#{username}, #{password}, #{email}, #{lastLoginTime}, #{updateTime}, #{createTime})
    </insert>
    <select id="getById" resultType="com.qiyuan.pojo.User">
        select *
        from user
        where id = #{id}
    </select>
    <select id="getByEmail" resultType="com.qiyuan.pojo.User">
        select *
        from user
        where email = #{email}
    </select>
    <update id="update">
        update user
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="email != null and email != ''">
                email = #{email},
            </if>
            <if test="lastLoginTime != null">
                last_login_time = #{lastLoginTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>
</mapper>
